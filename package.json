{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@storybook/storybook-deployer": "^2.8.16",
    "turbo": "^1.10.12"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}
